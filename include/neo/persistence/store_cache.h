#pragma once

#include <neo/persistence/data_cache.h>

namespace neo::persistence
{
    // Forward declaration - StoreCache is already defined in data_cache.h
    // This header exists for compatibility with test includes
}