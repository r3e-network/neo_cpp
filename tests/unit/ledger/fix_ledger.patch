--- a/tests/unit/ledger/test_ledger.cpp
+++ b/tests/unit/ledger/test_ledger.cpp
@@ -15,6 +15,9 @@ protected:
     void SetUp() override {
         // Initialize ledger with test configuration
         ledger_ = std::make_unique<Ledger>();
+        // Ensure proper initialization
+        if (!ledger_->IsInitialized())
+            ledger_->Initialize();
     }
 };
